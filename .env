# ----------------------------------------------------------
# Author: Nandan Kumar
# Date: 11/09/2025
# Assignment-10: Secure User Model (Pydantic Validation + Database Testing)
# File: .env
# ----------------------------------------------------------
# Description:
# Environment configuration for FastAPI application, database,
# and JWT security. Used automatically by Docker, pytest, and
# app/config.py during initialization.
# ----------------------------------------------------------

# ----------------------------------------------------------
# Application Environment
# ----------------------------------------------------------
ENV=development
APP_NAME=SecureUserModel
DEBUG=True
LOG_LEVEL=INFO

# ----------------------------------------------------------
# Database Configuration
# (Change credentials for your local PostgreSQL or Docker)
# ----------------------------------------------------------
DATABASE_URL=postgresql://user:password@localhost:5432/test_db

# ----------------------------------------------------------
# Security & JWT Configuration
# ----------------------------------------------------------
SECRET_KEY=super_secret_key_123
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ----------------------------------------------------------
# Docker / CI Settings (Optional for GitHub Actions)
# ----------------------------------------------------------
# These values help run database tests inside a container.
# You can leave them as default if using SQLite fallback.
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=test_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
