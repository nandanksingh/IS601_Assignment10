# ----------------------------------------------------------
# Author: Nandan Kumar
# Date: 11/09/2025
# Assignment-10: Secure User Model (Pydantic Validation + Database Testing)
# File: .env
# ----------------------------------------------------------
# Description:
# Centralized environment configuration for FastAPI app,
# database connection, JWT security, and Docker setup.
# Supports local testing (SQLite fallback) and Docker (Postgres).
# ----------------------------------------------------------

# ----------------------------------------------------------
# Application Environment
# ----------------------------------------------------------
ENV=development
APP_NAME=SecureUserModel
DEBUG=True
LOG_LEVEL=INFO

# ----------------------------------------------------------
# Database Configuration
# ----------------------------------------------------------
# When running inside Docker, host must be "db"
# When running locally (pytest), SQLite fallback is used automatically

POSTGRES_USER=ps_user
POSTGRES_PASSWORD=ps_password
POSTGRES_DB=fastapi_user_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Main database URL used by app/database/dbase.py
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Dedicated test database for CI/CD (optional)
TEST_DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/test_db

# ----------------------------------------------------------
# Security & JWT Configuration
# ----------------------------------------------------------
SECRET_KEY=super_secret_key_123
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ----------------------------------------------------------
# Docker / CI Settings (Used for pytest & GitHub Actions)
# ----------------------------------------------------------
PGADMIN_DEFAULT_EMAIL=admin@local.com
PGADMIN_DEFAULT_PASSWORD=admin
